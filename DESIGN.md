# 디자인 규약

웹 문서·계약 SaaS의 기준은 [apps/web/DESIGN.md](apps/web/DESIGN.md)와
`apps/web/src/styles/tokens.css`다. 모든 화면은 토큰과 공용 프리미티브를 사용한다.
확정 선택값은 `screen / web / content / none / general_consumer / unspecified / b2b_saas /
document_contract / neutral_minimal / document_viewer / light_dark`이며, 주색은 `#2563eb`다.
실행은 `pnpm install && pnpm dev` 한 명령 흐름으로 가능하고 목 데이터만 사용한다.
